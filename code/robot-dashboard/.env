# .env

# === Backend Configuration ===
# Port the Node.js server listens on INSIDE the container/environment
# This value is used by: server.js (process.env.PORT)
# Ensure this matches the CONTAINER port in your Docker mapping (e.g., 5000 if using -p 5001:5000)
PORT=5001


# === Frontend Configuration ===
# These MUST start with NEXT_PUBLIC_ to be exposed to the browser build by Next.js

# Full URL the BROWSER uses to connect to the Backend WebSocket endpoint.
# Use the HOST port exposed by Docker (e.g., 5001).
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:5001

# Full URL the BROWSER uses to connect to the Gradio interface.
# Use the HOST port where Gradio is accessible.
NEXT_PUBLIC_GRADIO_URL=http://localhost:7860

# === Other Configuration (Optional) ===
# Port Gradio is running on (if needed by backend/other processes, not directly for frontend URL construction)
# GRADIO_PORT=7860